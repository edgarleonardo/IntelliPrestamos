@model IEnumerable<IntelliPrestamos.Models.Reportes>
<div class="table-responsive">
    <table class="tblDataTble table table-striped table-hover">
        <caption>
            @if (ViewBag.DownloadLink != null)
            {
                <a href="../TmpFiles/@ViewBag.DownloadLink" class="btn btn-mint btn-rounded">Descargar Excel</a>
            }
        </caption>
        <thead>
            <tr>
                <th>Cedula</th>
                <th>Nombre</th>
                <th>Accionista</th>
                <th>Monto</th>
                <th># Prestamo</th>
                <th>Status</th>
                <th>Fecha Desembolso</th>
                <th>Fecha Cancelacion</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Cedula)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Nombre)
                    </td>
                    <td>@Html.DisplayFor(modelItem => item.Accinista_Nombre)</td>
                    <td>@item.monto_aprobado.ToString("0,0.000")</td>

                    <td>@Html.DisplayFor(modelItem => item.prestamo_id)</td>
                    <td>@Html.DisplayFor(modelItem => item.status_desc)</td>
                    <td>@Html.DisplayFor(modelItem => item.fecha_desembolso)</td>
                    <td>@Html.DisplayFor(modelItem => item.fecha_cancelacion)</td>

                </tr>
            }
        </tbody>
    </table>
</div>
<script type="text/javascript">
    $(function () {
        $('.tblDataTble').DataTable({
            "language": {
                "lengthMenu": "Mostrar _MENU_ Registros por Página",
                "zeroRecords": "No se encontraron coincidencias",
                "info": "Mostrando Página _PAGE_ de _PAGES_",
                "infoEmpty": "",
                "search": "Buscar:",
                "infoFiltered": "(filtered from _MAX_ total records)",
                "paginate": {
                    "first": "Primero",
                    "last": "Último",
                    "next": "Próximo",
                    "previous": "Anterior"
                }
            }
        });
    });
</script>